# 引入统一评估器 FactorEvaluator 串联治理约束

为了实现量化因子的契约化治理，我们决定在代码层引入统一的评估器接口 `FactorEvaluator` 并进行端到端流程测试。它将卡片合规校验、对抗冗余检验、基础诊断（IC/分桶）和分布漂移监控串联为一个完整的计算与准入决策闭环，并生成物源追溯记录。我们放弃了“仅提供零散分析函数并由研究员自由组合”的方案，以确保风控和准入门限在工程层面得到强制执行，避免统计过拟合因子漏网。
